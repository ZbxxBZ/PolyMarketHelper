version: '3.8'

services:
  polymarket:
    build: .
    container_name: polymarket-auto-trade
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
